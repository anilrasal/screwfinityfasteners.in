---
const { size='200', alt='Screwfinity Fasteners Logo',className = ""
 } = Astro.props;
const base = import.meta.env.BASE_URL; 
---
<img
  src={`${base}images/starlight/brand-logo-${size}.png`}
  width={size}
  height={size}
  alt={alt}
  class={className}
/>